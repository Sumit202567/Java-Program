#include <iostream>
using namespace std;

int main() {
    int i = 0;

    // Do-while loop
    do {
        cout << "Value of i: " << i << endl;
        i++;  // Increment i
    } while (i < 5);  // Condition to stop the loop

    return 0;
}
